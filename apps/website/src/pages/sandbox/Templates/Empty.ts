import { model, styles, views, workspace } from "@justmegaara/structurizr-dsl";

export const Empty = workspace(
    "Empty",
    "An empty diagram",
    model(),
    views([], [], [], [], styles())
);